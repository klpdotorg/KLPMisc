1. add config directory with klpconfig.ini file containing the following format:
[Database]
dbname=sys db
user=sys user
passwd=sys password

2. The images directory has a softlink sysimages which should be link to the folder where the sys images are stored. i.e. /images/sysimages should have folders sys and schoolpics_hash
